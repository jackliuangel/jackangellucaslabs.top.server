name: tests

on:
  push:
  pull_request:

jobs:
  test:
    name: Run test suites
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Tank game tests (var/www/html/tank)
        run: node var/www/html/tank/tests/tank.test.js

      - name: Video downloader script tests (ytdl)
        run: bash ytdl/tests/test_video_download_pro.sh

      - name: Shellcheck bash lint
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq shellcheck
          # -S error: 只拦截 error 级问题。脚本存在预先的 warning/info 级
          # 告警（SC2034/SC2128 等，位于原有 bilibili 分支），单独跟踪清理
          shellcheck -S error ytdl/video_download_pro.sh
